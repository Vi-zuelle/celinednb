<h1>Book a <%= @celineversion.name %></h1>
<%= simple_form_for([@celineversion, @booking]) do |f| %>
  <%= f.input :message, as: :text, placeholder: "Please write your message here" %>
  <%= f.input :start_date, as: :date, html5: true, label: "Booking From", wrapper_html: { class: "inline_field_wrapper" } %>
  <%= f.input :end_date, as: :date, html5: true, label: 'Booked Until'.html_safe, wrapper_html: { class: "inline_field_wrapper" } %>
  <%= f.submit "Submit", class: "btn btn-primary btn-lg btn-block btn-booking"%>
<% end %>

<h3>Price is : <%= @celineversion.price %></h3>
